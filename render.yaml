services:
  - type: web
    name: Meghshree-MeetPro
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class sync -w 1 --bind 0.0.0.0:$PORT --timeout 120 app:app
    envVars:
      - key: PORT
        value: 10000
    plan: free
